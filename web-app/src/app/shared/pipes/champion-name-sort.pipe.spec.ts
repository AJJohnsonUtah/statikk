import { ChampionNameSortPipe } from './champion-name-sort.pipe';

describe('ChampionNameSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ChampionNameSortPipe();
    expect(pipe).toBeTruthy();
  });
});
