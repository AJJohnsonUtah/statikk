import { ChampionSearchPipe } from './champion-search.pipe';

describe('ChampionSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ChampionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
