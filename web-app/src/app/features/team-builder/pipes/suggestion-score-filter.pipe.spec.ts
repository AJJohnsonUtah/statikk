import { SuggestionScoreFilterPipe } from './suggestion-score-filter.pipe';

describe('SuggestionScoreFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SuggestionScoreFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
